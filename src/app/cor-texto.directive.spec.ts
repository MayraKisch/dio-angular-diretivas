import { CorTextoDirective } from './cor-texto.directive';

describe('CorTextoDirective', () => {
  it('should create an instance', () => {
    const directive = new CorTextoDirective();
    expect(directive).toBeTruthy();
  });
});
